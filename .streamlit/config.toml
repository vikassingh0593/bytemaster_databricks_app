# Theme: run "streamlit run app.py" from the project root so this file is found.
# Project config path is: <current working directory>/.streamlit/config.toml
[theme]
base = "light"
primaryColor = "#007bff"